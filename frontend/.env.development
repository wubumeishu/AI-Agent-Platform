# P5MSG-06: 本地 AI-Agent-Platform 后端跑在 8001 (8000 是另一个批量注册工具, 不含 /api/v1/*)
VITE_API_BASE_URL=http://localhost:8001/api/v1
# P5MSG-06: 实时写端点 (POST /realtime/read | /publish) 与读端点
# (/realtime/conversations | /unread, P5MSG-FIX P0-2 + P1-3) 受信任生产者鉴权。
# 与后端 REALTIME_PUBLISH_TOKENS (backend/.env) 配置同值; 留空时相关端点返回 401
# (UI 有错误横幅, 会话列表/详情仍可浏览无预览数据)。
VITE_REALTIME_PUBLISH_TOKEN=p5msg-dev-producer-token
